export interface Product {
  code: number;
  name: string;
  productType?: string;
  size?: number;
  quantity: number;
  price: number;
  productCategory?: string;
  status: string;
  placeholder?: string;
}
